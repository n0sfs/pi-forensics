# Upper bounds added below (2026-08-23, Low finding #4 from the 2026-08-22
# security audit): Flask/gunicorn/cryptography previously had a floor with
# no ceiling - not an active vulnerability in any of the three (their known
# CVEs were already checked against this app's actual usage and don't
# apply), but an open-ended floor means a future `pip install -r
# requirements.txt` could silently pick up a new major release - a
# regression or a genuinely new vulnerability - with no review checkpoint
# prompting a look. reportlab is unaffected (already exact-pinned, ==4.0.8,
# no floor/ceiling question applies to it).
Flask>=3.0.0,<4.0.0
# >=23.0.0 specifically: CVE-2024-6827 is a TE.CL HTTP request-smuggling
# bug (Transfer-Encoding header not validated per RFC), fixed in 23.0.0 -
# confirmed via the Debian security tracker's own fixed-by commit
# reference, not just a changelog mention. Mitigated when nginx sits in
# front (this app's own recommended deployment - see the kiosk-bypass
# security-audit fix elsewhere in this history), more exposed on a plain-
# HTTP install with no reverse proxy. Found via the 2026-08-22 security
# audit. Ceiling <27.0.0: confirmed live against PyPI that 26.1.0 (already
# what's installed on the deployed Pi) is gunicorn's current latest -
# leaves room for that whole major's patch releases before a future major
# needs its own deliberate look.
gunicorn>=23.0.0,<27.0.0
psutil>=5.8.0
reportlab==4.0.8
mvt>=2.5.0
# >=20260715 specifically (not just "latest"): CVE-2026-40026 is an
# out-of-bounds-read + infinite-loop DoS in libtsk's ISO9660 SUSP parser,
# reachable via any pytsk3-backed route (Live Device Preview, Browse-as-
# Image, every in-image tool) on a deliberately crafted evidence image -
# exactly the anti-forensics scenario this appliance needs to be hardened
# against. Confirmed via pytsk3's own PyPI page that 20260715 bundles
# libtsk 4.15.0, the release that fixed this CVE (affected range is
# "through 4.14.0"). Found via the 2026-08-22 security audit.
pytsk3>=20260715
# Ceiling <55.0.0, deliberately wide: cryptography's own maintainers bump
# its major version on a rolling, roughly-every-few-weeks basis as a matter
# of normal (usually non-breaking) release practice, not as a semver
# breaking-change signal the way Flask/gunicorn do - confirmed live against
# PyPI that 50.0.0 is current latest (already several majors past this
# floor's 41.0.0, with 47.0.0 running on the deployed Pi right now) before
# picking this ceiling. A tight ceiling here would just mean this file
# needs re-touching every few weeks for no real security benefit; this
# still gives the audit's actual concern (an infinite, no-checkpoint floor)
# a real, finite bound.
cryptography>=41.0.0,<55.0.0
volatility3>=2.11.0
# Windows Registry hive / Event Log (.evtx) / .lnk shortcut parsing
# (Part C of the case-management expansion) - all three are pure-Python,
# confirmed installing cleanly (piwheels index, no compile step) on the
# Pi's real ARM64/Debian-trixie venv before being added here.
python-registry>=1.3.1
python-evtx>=0.8.1
LnkParse3>=1.6.0
# Renders the in-app doc viewer (Help > Quick-Start/User Manual/Release
# Notes; Settings > Diagnostics > View Release Notes) from the shipped
# docs/*.md + CHANGELOG.md into real styled HTML instead of raw markdown
# text - confirmed live on the real Pi venv (arm64/Debian trixie) before
# adding: pure-Python wheel, zero native deps, installs cleanly.
Markdown>=3.6
# YARA rule scanning (Part D, D3 - the last item of the case-management
# expansion). Unlike C1-C3's pure-Python parsers, this bundles native
# libyara via its own wheel - confirmed live on the real Pi venv (arm64/
# Debian trixie) that a prebuilt manylinux2014_aarch64 wheel installs with
# no compile step (yara_python-4.5.4-cp313-cp313-manylinux_2_17_aarch64...),
# same "verify before committing" discipline this project has applied to
# every native/compiled dependency before it.
yara-python>=4.5.0
# Windows Prefetch (.pf) parsing (Part C follow-up, 2026-08-25) - like
# yara-python above, bundles a native library (libscca) via its own wheel,
# not pure Python. Confirmed live on the real Pi venv (arm64/Debian trixie)
# that a prebuilt manylinux2014_aarch64 wheel installs with no compile step
# (libscca_python-20260527-cp313-cp313-manylinux2014_aarch64...) before
# being added here. Recycle Bin parsing (the other artifact type added
# alongside this) needed no new dependency at all - see
# core/recyclebin_utils.py's own module docstring for why.
libscca-python>=20220717
# Hardens KML parsing (routes/reporting.py's _parse_kml_placemarks(), which
# parses examiner-uploaded/hand-edited/third-party .kml files, not just
# ones this app generated) against billion-laughs/XXE-style XML entity
# attacks, independent of whatever billion-laughs protection the deployed
# system's own libexpat build may or may not have (2026-08-22 security
# audit, Informational finding, closed 2026-08-25). Confirmed live on the
# real Pi venv: pure-Python wheel, zero native deps, installs cleanly, and
# a drop-in replacement for xml.etree.ElementTree (ParseError is literally
# the same class, re-exported).
defusedxml>=0.7.1
# SQLite Dissect (mobile-forensics-tool gap-closing round, 2026-08-30) -
# recovers deleted rows from a SQLite file's freeblocks/unallocated space/
# any surviving WAL file. Confirmed live on the real Pi venv (arm64/Debian
# trixie): pure-Python py3-none-any wheel, zero native deps, installs with
# no compile step. Wrapped as a subprocess (core/sqlite_dissect_utils.py) -
# see that module's own docstring for the real, live-confirmed CLI flags
# (genuinely different from an earlier guess) and an honest disclosure
# about how deletion-recovery reliability depends heavily on the specific
# acquisition scenario, confirmed via real hand-built test cases this
# session.
sqlite-dissect==1.0.0
# androguard (mobile-forensics-tool gap-closing round, feature 2) - static
# analysis of Android .apk files (permissions, manifest components, signing
# certificates, embedded-URL scan). Confirmed live on the real Pi venv
# (arm64/Debian trixie): pure-Python wheel, installs cleanly (4.1.4).
# Imported directly (core/apk_utils.py), not subprocess-wrapped - its
# `androguard.core.apk.APK` class is a long-stable, widely-documented
# public API, unlike sqlite-dissect's own unconfirmed Python surface above.
# One real, load-bearing correction found via live testing against a real
# signed APK before writing any code: a signing-certificate object is
# `asn1crypto.x509.Certificate`, not `cryptography.x509.Certificate` - see
# core/apk_utils.py's own module docstring for the exact attribute-access
# difference this required.
androguard>=4.1.0
# wa-crypt-tools (mobile-forensics-tool gap-closing round, feature 3) -
# decrypts a WhatsApp local backup (msgstore.db.crypt12/14/15) against the
# device's own key file. Confirmed live on the real Pi venv (arm64/Debian
# trixie): pure-Python wheel + a native pycryptodomex manylinux aarch64
# wheel, no compile step. Wrapped as a subprocess
# (core/whatsapp_utils.py) - see that module's own docstring for the
# real, live-confirmed positional argument order (verified via a real
# synthetic key+crypt14 round trip) and a genuine upstream bug found
# along the way in this package's own `wacreatekey` tool (unrelated to
# what this app actually calls, but worth knowing about).
wa-crypt-tools>=0.1.0
# lief (mobile-forensics-tool gap-closing round, feature 4) - optional
# Mach-O binary analysis layer for iOS .ipa static analysis
# (core/ipa_utils.py's plist/mobileprovision core is stdlib-only and
# needs no dependency at all; this is only for the architecture/
# FairPlay-encryption-status layer on top). Confirmed live on the real
# Pi venv (arm64/Debian trixie): a real prebuilt manylinux_2_28_aarch64
# wheel from PyPI directly (not piwheels), no compile step - resolves
# this feature's own plan-stage uncertainty about aarch64 wheel
# availability in the affirmative.
lief>=1.0.0
# dumpstate-py (mobile-forensics-tool gap-closing round, feature 5) -
# deep-parses an already-captured adb bugreport zip (mount points,
# process list, package install log, kernel modules, GPS, crash traces,
# sockets, battery, power events) into structured sections. Not on PyPI
# - installed via pip's own PEP 508 direct-reference syntax, pinned to a
# specific commit (matching this project's own established ALEAPP/iLEAPP
# discipline of never installing an unpinned `main`/HEAD from a small,
# actively-developed external project). Confirmed live on the real Pi
# venv (arm64/Debian trixie): pure-Python, only depends on `rich`
# (already present), installs cleanly with no compile step - both via a
# direct `pip install git+...@<sha>` and via this exact requirements.txt
# line's own PEP 508 syntax. The real importable module name is
# `dumpstate` (not `dumpstate_py` - the hyphenated pip/PyPI-style
# package name doesn't match its own import name), confirmed via direct
# introspection before writing core/bugreport_utils.py.
dumpstate-py @ git+https://github.com/CERT-EDF/dumpstate-py.git@56b70934f70a19f50f96d337f96bf507ac9cc65c

# analyzeMFT (2026-08-30, tool-survey follow-up) - NTFS $MFT parsing with
# timestomping detection (core/mft_utils.py wraps its `analyzemft` console
# script). Confirmed live installable as a pure-Python wheel on the Pi's
# real ARM64/Debian-trixie venv - zero compile step, only depends on
# openpyxl (already present via wa-crypt-tools/other tools in this file).
analyzeMFT>=3.1.1

# libpff-python (2026-08-30, tool-survey follow-up) - Outlook PST/OST email
# parsing (core/email_utils.py). Confirmed live on the Pi's real ARM64/
# Debian-trixie venv: source-only (no prebuilt wheel), but compiles cleanly
# with no external system dependency needed beyond what's already present.
libpff-python>=20231205

# py-tlsh (2026-08-30, tool-survey follow-up) - fuzzy hashing
# (core/fuzzy_hash_utils.py). Confirmed live on the Pi's real ARM64/
# Debian-trixie venv: source-only (a small C extension), compiles cleanly
# with no external system dependency needed.
py-tlsh>=5.0.0
